package edu.harvard.wcfia.yoshikoder.document.tokenizer;

import java.util.Iterator;

public class ConcordanceSpanList {

    /*
    
    protected SpanList lhs;
    protected SpanList target;
    protected SpanList rhs;
    
    class Iter implements Iterator{
        int index = -1;
  
        Iter(){
            if (spans != null)
                index = 0;
        }
        
        public boolean hasNext() {
            return (index > -1 && index < spans.length);
        }

        public Object next() {
            return spans[index++];
        }

        public void remove() {
            throw new UnsupportedOperationException("Cannot remove from inside a SpanList iterator");
        };
    };
    
    public ConcordanceSpanList(){
        //
    }
    
    public ConcordanceSpanList(SpanList left, SpanList middle, SpanList right){
        lhs = left;
        target = middle;
        rhs = right;
    }
    
    */
}
